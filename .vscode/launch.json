{
    // Используйте IntelliSense, чтобы узнать о возможных атрибутах.
    // Наведите указатель мыши, чтобы просмотреть описания существующих атрибутов.
    // Для получения дополнительной информации посетите: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Cortex Debug",
            "cwd": "${workspaceRoot}",
            "executable": "${workspaceRoot}/out/main.elf",
            "request": "launch",
            "type": "cortex-debug",
            "device": "STM32F103C8",
            "servertype": "openocd",
            "interface": "swd",
            "configFiles": ["${workspaceRoot}/stm32f103c8t6.cfg"],
            "svdFile": "${workspaceRoot}/stm32f103c8t6.svd"
        }
    ]
}